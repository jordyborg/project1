<%= form_for @artist do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :nationality %>
<%= f.text_field :nationality %>

<%= f.label :skillmove, 'elastico' %>
<%= f.text_field :skillmove, :placeholder => 'What is your tekkerz?' %>

<%= f.submit %>

<% end %>